{
  "name": "database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "author": "Gunnar Oledal",
  "license": "MIT",
  "prisma": {
    "seed": "ts-node ./seed.ts",
    "schema": "./prisma/schema.prisma"
  },
  "scripts": {
    "build": "tsc --build",
    "migrate:deploy": "dotenv -e ../../.env -c -- prisma migrate deploy",
    "db:generate": "prisma generate",
    "migrate": "dotenv -e ../../.env -c -- prisma migrate dev",
    "migrate:reset": "dotenv -e ../../.env -c -- prisma migrate reset",
    "seed": "dotenv -e ../../.env -c -- prisma db seed",
    "studio": "dotenv -e ../../.env -c -- prisma studio",
    "dbpush": "dotenv -e ../../.env -c -- prisma db push"
  },
  "devDependencies": {
    "prisma": "3",
    "ts-node": "^10.7.0",
    "@types/bcrypt": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "3",
    "bcrypt": "^5.0.1",
    "shared-types": "workspace:*"
  }
}
